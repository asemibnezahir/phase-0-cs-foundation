#include<stdio.h>
/*
অনেক বেশি ভেরিয়েল প্রয়োজন হলে আমরা একটি এরে নিয়ে নেই, ঠিক সেমভাবে
অনেক বেশি এরে প্রয়োজন হলে আমরা একটি 2D Array নিতে পারি।
*/

/*
এরের প্রতিটা ঘরে যেমন একটা ভেরিয়েবলের মতো কাজ করে, তেমন 2D Array
এর প্রতিটা ঘর একটা এরের মতো কাজ করে।
*/

/*
একটা 2D Array এর ভিতরে থাকা সবগুলো এরে এর সাইজ সমান হয়, 
একেকটা একেক সাইজের এরে নেয়া যায়না।
*/
int main()
{
    
    return 0;
}