#include<stdio.h>
int main()
{
    //If we accidently give wrong logic in condition,
    //then we can face infinite loop which can be cause of crash,
    //when it happens,We Should close the terminal by ctrl+c, then 
    //delete the output & make correction on the condition before
    //run the code again.
    return 0;
}