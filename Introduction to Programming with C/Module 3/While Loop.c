#include<stdio.h>
int main()
{
    /*While Loop Syntax:

    Initialization
    while(end condition)
    {
     Code
     encryment/decryment
    }

     */
    int i=1;
    while (i<11)
    {
        printf("%d\n",i);
        i++;
    }
    //While Loop & For Loop works same, just their syntax different
    
    return 0;
}